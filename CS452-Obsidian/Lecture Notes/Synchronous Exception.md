System calls, SVC. Synchronous in respect to instruction execution. It is cased by an instruction that the program executes. The program knows it. 

### System Call
When a program requests resource from the operating system. 

